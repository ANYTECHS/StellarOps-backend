# sample-feature

Non-wired example feature module showing the scalable folder architecture:

- controllers
- services
- dtos
- guards
- interceptors

This module is intentionally not imported by `AppModule` to avoid runtime changes.
